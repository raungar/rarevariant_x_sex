# RVSex
Rare Variants and Sex


Scripts/get_residuals_peer_by_sex.R 
   >>  Call using: Rscript Scripts/get_residuals_peer_by_sex.R --dir_peer "RAREDIR/preprocessing_v8/PEER_v8" -m "$GTEX_SUBJECTSv8"
   >> -d or --dir_peer is peer directory, -m or --metadata is metadata, -s or --subset_do is to subset to males/females/both (default is yes, options are y/yes/n/no), -r --residuals_do is to calculate the residuals (default is yes, options are y/yes/n/no)
